## 太好了！我们已经完成了以下任务：

1. 合并了两个重叠的服务：`src/lib/server/mail-service-enhanced.ts` 和 `src/lib/services/enhanced-mail-service.ts`
2. 保留了 `src/lib/services/enhanced-mail-service.ts` 作为主要实现，并增强了它的功能
3. 将 `src/lib/server/mail-service-enhanced.ts` 转换为兼容性包装器，导出统一服务的方法
4. 更新了依赖的 API 路由，使用统一的服务

新的 `EnhancedMailService` 具有以下优势：

1. **服务器和客户端缓存统一**：根据运行环境（服务器或客户端）自动选择合适的缓存机制
2. **更清晰的代码结构**：各个方法职责明确，分离了服务器和客户端逻辑
3. **更好的错误处理**：统一的日志格式，使用 `[EnhancedMailService]` 前缀标识所有日志
4. **强化的缓存失效机制**：在适当的操作（如标记已读/未读）后自动使相关缓存失效
5. **兼容现有代码**：保留了旧服务的导出接口，确保现有代码不会因为重构而中断

这次重构使得邮件服务更加统一、易于维护。前端代码不需要修改，因为它们已经正确地导入了 `src/lib/services/enhanced-mail-service.ts`。

您可以考虑进一步改进：

1. 完善 `markAsRead` 和 `markAsUnread` 方法，实现真实的 Nylas API 调用
2. 添加更多邮件操作功能，如删除、移动到文件夹等
3. 改进缓存刷新策略，例如实现基于时间的自动后台刷新
4. 增加更详细的日志和监控机制

需要其他修改或改进吗？




<think>我需要查看两个文件：`src/lib/server/mail-service-enhanced.ts` 和 `src/lib/services/enhanced-mail-service.ts`，确认它们的功能重叠情况，然后决定如何统一为一个服务，并更新所有引用的地方。

首先让我查看这两个文件的内容：</think>我需要查看两个服务文件的内容，以确定它们的功能重叠情况，并计划如何统一为一个增强型服务。

Read file: src/lib/server/mail-service-enhanced.ts

Read file: src/lib/services/enhanced-mail-service.ts
现在我需要查看是否有任何文件引用了这两个服务，以了解需要更新哪些地方：

Ran tool

Ran tool

Ran tool
接下来，我还需要查看 `use-enhanced-mail-queries.ts` 文件，了解它是如何使用增强型邮件服务的：

Read file: src/hooks/use-enhanced-mail-queries.ts
现在我需要查看引用 `EnhancedMailService` 的 API 路由文件：

Read file: src/app/api/mail/inbox/route.ts

Read file: src/app/api/mail/message/[id]/route.ts
最后，我还需要看看服务器端的缓存管理器实现：

Read file: src/lib/server/cache-manager.ts

Read file: src/lib/db/mail-db.ts
让我们了解一下 Nylas 服务的实现：

Read file: src/lib/services/nylas-service.ts

Read file: src/lib/server/mail-service.ts
根据我的分析，我会整合这两个服务，并更新所有引用的地方。以下是我的计划：

1. 将 `src/lib/server/mail-service-enhanced.ts` 和 `src/lib/services/enhanced-mail-service.ts` 合并为一个统一的服务
2. 保留 `src/lib/services/enhanced-mail-service.ts` 作为主要实现，因为它功能更完整（包含IndexedDB缓存和标记邮件为已读/未读）
3. 将服务器端缓存功能从 `mail-service-enhanced.ts` 移植到 `enhanced-mail-service.ts`
4. 更新引用了 `mail-service-enhanced.ts` 的API路由

让我开始实现：

```typescript:src/lib/services/enhanced-mail-service.ts
import { EmailMessage, NylasAuthData } from "../types/nylas-types";
import mailAccountService from "./mail-account-service";
import nylasService from "./nylas-service";
import { mailDB, EmailMessageCache } from "../db/mail-db";
import cacheManager from "../server/cache-manager";

// 定义邮件查询选项接口
export interface MailQueryOptions {
  limit?: number;
  offset?: number;
  unread?: boolean;
  folder?: string;
  forceRefresh?: boolean;
}

/**
 * MARK: 增强版邮件服务
 * 增强型邮件服务 - 整合在线API、本地IndexedDB缓存和服务器内存缓存
 */
export class EnhancedMailService {
  // 缓存刷新时间间隔（分钟）
  private readonly cacheRefreshInterval = 5;
  // 是否在服务器环境
  private readonly isServer = typeof window === 'undefined';

  /**
   * MARK: 获取邮件列表
   * @param options 查询选项
   */
  async getEmails(options: MailQueryOptions = {}): Promise<EmailMessage[]> {
    const {
      limit = 20,
      offset = 0,
      unread,
      folder = "inbox",
      forceRefresh = false,
    } = options;

    // 获取当前账户
    const currentAccount = mailAccountService.getCurrentAccount();
    if (!currentAccount) {
      console.log("没有当前账户");
      return [];
    }
    
    // 服务器端和客户端使用不同的缓存策略
    if (this.isServer) {
      return await this.getEmailsServer(currentAccount.grantId, options);
    } else {
      return await this.getEmailsClient(currentAccount.grantId, options);
    }
  }

  /**
   * 服务器端获取邮件列表（使用内存缓存）
   */
  private async getEmailsServer(accountId: string, options: MailQueryOptions = {}): Promise<EmailMessage[]> {
    const { limit = 20, offset = 0, unread, forceRefresh = false } = options;
    const cacheKey = `emails:${accountId}:${limit}:${offset}:${unread}`;

    // 如果强制刷新或处于开发环境，跳过缓存
    if (forceRefresh || process.env.NODE_ENV === 'development') {
      console.log(`[EnhancedMailService] 跳过缓存，直接从API获取邮件`);
      const data = await this.fetchEmailsFromApi(accountId, { limit, offset, unread });
      // 更新缓存
      cacheManager.set(cacheKey, data, 2 * 60 * 1000); // 缓存2分钟
      return data;
    }

    // 尝试从缓存获取
    const cachedData = cacheManager.get<EmailMessage[]>(cacheKey);
    if (cachedData) {
      console.log(`[EnhancedMailService] 服务器缓存命中: ${cacheKey}`);
      return cachedData;
    }

    // 缓存未命中，从API获取
    console.log(`[EnhancedMailService] 服务器缓存未命中: ${cacheKey}`);
    const data = await this.fetchEmailsFromApi(accountId, { limit, offset, unread });
    
    // 缓存结果
    cacheManager.set(cacheKey, data, 2 * 60 * 1000); // 缓存2分钟
    
    return data;
  }

  /**
   * 客户端获取邮件列表（使用IndexedDB缓存）
   */
  private async getEmailsClient(accountId: string, options: MailQueryOptions = {}): Promise<EmailMessage[]> {
    const { limit = 20, offset = 0, unread, folder = "inbox", forceRefresh = false } = options;

    try {
      // 尝试从IndexedDB缓存获取数据
      if (!forceRefresh) {
        const cachedEmails = await this.getEmailsFromCache(
          accountId,
          { limit, offset, unread, folder }
        );

        // 如果有缓存数据且未过期，则返回缓存
        if (cachedEmails.length > 0) {
          console.log(`[EnhancedMailService] 客户端缓存命中: ${cachedEmails.length}封邮件`);

          // 在后台刷新缓存
          this.refreshCacheIfNeeded(accountId, { unread, folder });

          return cachedEmails;
        }
      }

      // 缓存不可用或强制刷新，从API获取数据
      console.log(`[EnhancedMailService] 客户端缓存未命中，从API获取邮件: ${accountId}`);
      
      // 从API获取邮件
      const emails = await this.fetchEmailsFromApi(accountId, { limit, offset, unread });

      // 缓存结果
      await this.cacheEmails(emails, accountId, folder);

      return emails;
    } catch (error) {
      console.error("[EnhancedMailService] 获取邮件失败:", error);

      // 如果API请求失败，尝试使用缓存数据（即使可能已过期）
      const cachedEmails = await this.getEmailsFromCache(
        accountId,
        { limit, offset, unread, folder }
      );

      if (cachedEmails.length > 0) {
        console.log(`[EnhancedMailService] API失败，使用缓存中的 ${cachedEmails.length} 封邮件`);
        return cachedEmails;
      }

      throw error;
    }
  }

  /**
   * 从API获取邮件
   */
  private async fetchEmailsFromApi(accountId: string, options: { limit?: number; offset?: number; unread?: boolean }): Promise<EmailMessage[]> {
    // 构造API查询选项
    const apiOptions: any = {
      limit: options.limit || 20,
      offset: options.offset || 0,
    };

    if (options.unread !== undefined) {
      apiOptions.unread = options.unread;
    }

    // 调用Nylas API获取邮件
    return await nylasService.getEmails(accountId, apiOptions);
  }

  /**
   * MARK: 获取单封邮件详情
   * @param messageId 邮件ID
   * @param forceRefresh 是否强制刷新
   */
  async getEmail(
    messageId: string,
    forceRefresh = false
  ): Promise<EmailMessage | null> {
    if (!messageId) {
      console.log("[EnhancedMailService] 邮件ID不能为空");
      return null;
    }

    // 获取当前账户
    const currentAccount = mailAccountService.getCurrentAccount();
    if (!currentAccount) {
      console.log("[EnhancedMailService] 没有当前账户");
      return null;
    }

    // 服务器端和客户端使用不同的缓存策略
    if (this.isServer) {
      return await this.getEmailServer(currentAccount.grantId, messageId, forceRefresh);
    } else {
      return await this.getEmailClient(currentAccount.grantId, messageId, forceRefresh);
    }
  }

  /**
   * 服务器端获取单封邮件（使用内存缓存）
   */
  private async getEmailServer(accountId: string, messageId: string, forceRefresh = false): Promise<EmailMessage | null> {
    const cacheKey = `email:${messageId}`;

    // 如果强制刷新或处于开发环境，跳过缓存
    if (forceRefresh || process.env.NODE_ENV === 'development') {
      console.log(`[EnhancedMailService] 跳过缓存，直接从API获取邮件详情`);
      const email = await nylasService.getEmail(accountId, messageId);
      // 更新缓存
      if (email) {
        cacheManager.set(cacheKey, email, 5 * 60 * 1000); // 缓存5分钟
      }
      return email;
    }

    // 尝试从缓存获取
    const cachedData = cacheManager.get<EmailMessage>(cacheKey);
    if (cachedData) {
      console.log(`[EnhancedMailService] 服务器缓存命中: ${cacheKey}`);
      return cachedData;
    }

    // 缓存未命中，从API获取
    console.log(`[EnhancedMailService] 服务器缓存未命中: ${cacheKey}`);
    const email = await nylasService.getEmail(accountId, messageId);
    
    // 缓存结果
    if (email) {
      cacheManager.set(cacheKey, email, 5 * 60 * 1000); // 缓存5分钟
    }
    
    return email;
  }

  /**
   * 客户端获取单封邮件（使用IndexedDB缓存）
   */
  private async getEmailClient(accountId: string, messageId: string, forceRefresh = false): Promise<EmailMessage | null> {
    try {
      // 尝试从缓存获取
      if (!forceRefresh) {
        const cachedEmail = await mailDB.getCachedEmail(messageId);
        if (cachedEmail) {
          console.log(`[EnhancedMailService] 客户端缓存命中: ${messageId}`);
          return cachedEmail;
        }
      }

      // 缓存不可用或强制刷新，从API获取
      console.log(`[EnhancedMailService] 客户端缓存未命中，从API获取邮件详情: ${messageId}`);
      const email = await nylasService.getEmail(accountId, messageId);

      // 缓存单封邮件
      if (email) {
        await mailDB.cacheEmails([email], accountId);
      }

      return email;
    } catch (error) {
      console.error(`[EnhancedMailService] 获取邮件 ${messageId} 详情失败:`, error);

      // 如果API请求失败，尝试使用缓存
      const cachedEmail = await mailDB.getCachedEmail(messageId);
      if (cachedEmail) {
        console.log(`[EnhancedMailService] API失败，使用缓存中的邮件 ${messageId}`);
        return cachedEmail;
      }

      return null;
    }
  }

  /**
   * MARK: 获取账户信息
   */
  async getAccount(): Promise<any> {
    // 获取当前账户
    const currentAccount = mailAccountService.getCurrentAccount();
    if (!currentAccount) {
      console.log("[EnhancedMailService] 没有当前账户");
      return null;
    }

    // 服务器端和客户端使用不同的缓存策略
    if (this.isServer) {
      return await this.getAccountServer(currentAccount.grantId);
    } else {
      // 客户端直接返回当前账户信息
      return currentAccount;
    }
  }

  /**
   * 服务器端获取账户信息（使用内存缓存）
   */
  private async getAccountServer(accountId: string): Promise<any> {
    const cacheKey = `account:${accountId}`;

    // 尝试从缓存获取
    const cachedData = cacheManager.get(cacheKey);
    if (cachedData) {
      console.log(`[EnhancedMailService] 服务器缓存命中: ${cacheKey}`);
      return cachedData;
    }

    // 缓存未命中，从API获取
    console.log(`[EnhancedMailService] 服务器缓存未命中: ${cacheKey}`);
    try {
      const account = await nylasService.getEmailAccountInfo(accountId);
      
      // 缓存结果
      cacheManager.set(cacheKey, account, 30 * 60 * 1000); // 缓存30分钟
      
      return account;
    } catch (error) {
      console.error("[EnhancedMailService] 获取账户信息失败:", error);
      return null;
    }
  }

  /**
   * MARK: 标记邮件为已读
   * @param messageId 邮件ID
   */
  async markAsRead(messageId: string): Promise<boolean> {
    try {
      // 获取当前账户
      const currentAccount = mailAccountService.getCurrentAccount();
      if (!currentAccount) {
        console.log("[EnhancedMailService] 没有当前账户");
        return false;
      }

      // TODO: 实现API调用标记为已读
      // 这里需要实现真实的Nylas API调用

      // 更新本地缓存
      await mailDB.updateEmailReadStatus(messageId, true);

      // 使服务器端缓存失效
      this.invalidateEmailCache(messageId);

      return true;
    } catch (error) {
      console.error(`[EnhancedMailService] 标记邮件 ${messageId} 为已读失败:`, error);
      return false;
    }
  }

  /**
   * MARK: 标记邮件为未读
   * @param messageId 邮件ID
   */
  async markAsUnread(messageId: string): Promise<boolean> {
    try {
      // 获取当前账户
      const currentAccount = mailAccountService.getCurrentAccount();
      if (!currentAccount) {
        console.log("[EnhancedMailService] 没有当前账户");
        return false;
      }

      // TODO: 实现API调用标记为未读
      // 这里需要实现真实的Nylas API调用

      // 更新本地缓存
      await mailDB.updateEmailReadStatus(messageId, false);

      // 使服务器端缓存失效
      this.invalidateEmailCache(messageId);

      return true;
    } catch (error) {
      console.error(`[EnhancedMailService] 标记邮件 ${messageId} 为未读失败:`, error);
      return false;
    }
  }

  /**
   * MARK: 刷新邮件缓存
   */
  async refreshCache(options: MailQueryOptions = {}): Promise<boolean> {
    const currentAccount = mailAccountService.getCurrentAccount();
    if (!currentAccount) {
      console.log("[EnhancedMailService] 没有当前账户");
      return false;
    }

    try {
      // 强制从API获取最新邮件
      const emails = await this.getEmails({
        ...options,
        forceRefresh: true,
      });

      console.log(`[EnhancedMailService] 刷新缓存，获取 ${emails.length} 封邮件`);

      // 使服务器端缓存失效
      this.invalidateEmailsCache();

      return true;
    } catch (error) {
      console.error("[EnhancedMailService] 刷新缓存失败:", error);
      return false;
    }
  }

  /**
   * MARK: 清理过期缓存
   */
  async cleanupCache(): Promise<number> {
    try {
      // 清理客户端IndexedDB缓存
      const count = await mailDB.clearStaleCache(30); // 清理30分钟以上的缓存
      
      // 注意：服务器端内存缓存会自动过期，无需手动清理
      
      return count;
    } catch (error) {
      console.error("[EnhancedMailService] 清理缓存失败:", error);
      return 0;
    }
  }

  /**
   * 使邮件列表缓存失效
   */
  invalidateEmailsCache() {
    console.log("[EnhancedMailService] 使邮件列表缓存失效");
    if (this.isServer) {
      cacheManager.invalidate("emails:");
    }
    // 客户端缓存通过refreshCache处理
  }

  /**
   * 使特定邮件的缓存失效
   */
  invalidateEmailCache(emailId: string) {
    console.log(`[EnhancedMailService] 使邮件 ${emailId} 缓存失效`);
    if (this.isServer) {
      cacheManager.invalidate(`email:${emailId}`);
    }
    // 客户端缓存将在下次访问时自动更新
  }

  /**
   * 从客户端缓存获取邮件
   */
  private async getEmailsFromCache(
    accountId: string,
    options: MailQueryOptions
  ): Promise<EmailMessageCache[]> {
    return await mailDB.getCachedEmails(accountId, options);
  }

  /**
   * 缓存邮件到客户端
   */
  private async cacheEmails(
    emails: EmailMessage[],
    accountId: string,
    folder: string = "inbox"
  ): Promise<void> {
    if (!emails || emails.length === 0) return;
    await mailDB.cacheEmails(emails, accountId, folder);
  }

  /**
   * 在需要时刷新客户端缓存
   */
  private async refreshCacheIfNeeded(
    accountId: string,
    options: { unread?: boolean; folder?: string } = {}
  ): Promise<void> {
    // 这里可以添加逻辑来决定是否需要在后台刷新缓存
    // 例如，检查缓存的最后更新时间
  }
}

// 创建单例实例
const enhancedMailService = new EnhancedMailService();
export default enhancedMailService;
```
现在，让我们更新原来的 `src/lib/server/mail-service-enhanced.ts` 文件，将其转换为一个简单的导出，指向我们更新后的统一服务：

```typescript:src/lib/server/mail-service-enhanced.ts
/**
 * 服务器增强型邮件服务
 * 
 * 注意: 此文件为兼容性包装，实际实现已移至 @/lib/services/enhanced-mail-service.ts
 * 为确保代码一致性和减少重复，请使用新的统一服务
 */

import enhancedMailService from "@/lib/services/enhanced-mail-service";

// 重新导出增强型邮件服务（为了兼容性）
export const { 
  getEmails, 
  getEmail, 
  getAccount,
  invalidateEmailsCache,
  invalidateEmailCache 
} = enhancedMailService;

// 导出服务实例
export { enhancedMailService };
```
现在，我们需要更新 API 路由，以使用我们新的统一服务：

```typescript:src/app/api/mail/inbox/route.ts
import { NextResponse } from "next/server";
import enhancedMailService from "@/lib/services/enhanced-mail-service";

// 为请求创建一个简单的缓存标识
const generateCacheKey = (limit: number, offset: number, unread: boolean) => {
  return `emails:${limit}:${offset}:${unread}`;
};

// 请求计数器，用于在开发模式下检测多次请求
const requestCounts: Record<string, number> = {};

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const limit = parseInt(searchParams.get("limit") || "20");
  const offset = parseInt(searchParams.get("offset") || "0");
  const unread = searchParams.get("unread") === "true";

  // 创建缓存键
  const cacheKey = generateCacheKey(limit, offset, unread);

  // 开发模式下记录请求次数
  if (process.env.NODE_ENV === "development") {
    requestCounts[cacheKey] = (requestCounts[cacheKey] || 0) + 1;
    console.log(`[API] 邮件请求 #${requestCounts[cacheKey]}: ${cacheKey}`);
  }

  try {
    console.log(
      `[API] 获取邮件: limit=${limit}, offset=${offset}, unread=${unread}`
    );

    // 使用增强版邮件服务
    const data = await enhancedMailService.getEmails({
      limit,
      offset,
      unread,
    });

    console.log(`[API] 成功获取邮件: ${data.length || 0}封`);

    // 创建响应对象
    const response = NextResponse.json(data);

    // 添加缓存控制头
    response.headers.set("Cache-Control", "public, max-age=300"); // 缓存5分钟
    response.headers.set("X-Cache-Key", cacheKey);

    // 直接返回API的响应，保持数据结构与前端期望的一致
    return response;
  } catch (error) {
    console.error("[API] 获取邮件出错:", error);
    return NextResponse.json(
      {
        error: "获取邮件失败",
        message: error instanceof Error ? error.message : "未知错误",
      },
      { status: 500 }
    );
  }
}
```

```typescript:src/app/api/mail/message/[id]/route.ts
import { NextResponse } from "next/server";
import enhancedMailService from "@/lib/services/enhanced-mail-service";

// 请求计数器，用于在开发模式下检测重复请求
const requestCounts: Record<string, number> = {};

export async function GET(
  request: Request,
  { params }: { params: { id: string } }
) {
  // 确保params是可用的
  if (!params || typeof params.id !== "string") {
    return NextResponse.json({ error: "邮件ID无效" }, { status: 400 });
  }

  const emailId = params.id;

  // 在开发模式下记录请求
  if (process.env.NODE_ENV === "development") {
    requestCounts[emailId] = (requestCounts[emailId] || 0) + 1;
    console.log(`[API] 邮件详情请求 #${requestCounts[emailId]}: ${emailId}`);
  }

  try {
    console.log(`[API] 获取邮件详情: id=${emailId}`);

    // 使用增强版邮件服务
    const message = await enhancedMailService.getEmail(emailId);

    if (!message) {
      return NextResponse.json({ error: "邮件不存在" }, { status: 404 });
    }

    console.log(`[API] 成功获取邮件详情: id=${emailId}`);

    // 创建响应对象
    const response = NextResponse.json(message);

    // 添加缓存控制头 - 缓存10分钟
    response.headers.set("Cache-Control", "public, max-age=600");
    response.headers.set("X-Email-Id", emailId);

    // 返回响应
    return response;
  } catch (error) {
    console.error("[API] 获取邮件详情出错:", error);
    return NextResponse.json(
      {
        error: "获取邮件详情失败",
        message: error instanceof Error ? error.message : "未知错误",
      },
      { status: 500 }
    );
  }
}
```
最后，检查一下前端组件如何导入增强型邮件服务：

Read file: src/hooks/use-enhanced-mail-queries.ts
